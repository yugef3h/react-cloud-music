### 打开方式

```

$ git clone https://github.com/yugef3h/react-cloud-music.git
$ cd cloud-music
$ npm install

// 下载子模块
$ git submodule update --init --recursive
$ cd NeteaseCloudMusicApi
$ npm install
$ cd ../

// 运行
$ npm run start
```